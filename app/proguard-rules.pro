# Add project specific ProGuard rules here.
# Triledge keeps minify on for release. Most rules come from the consumer
# .pro files of the AndroidX libraries we depend on; this file is for
# app-specific keeps only. Empty for now.