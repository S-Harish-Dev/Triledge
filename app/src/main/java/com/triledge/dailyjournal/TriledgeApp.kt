package com.triledge.dailyjournal

import android.app.Application

/**
 * Application class. Reserved for future single-instance initialization
 * (DataStore is lazy via the repo, no eager init needed in Step 1).
 */
class TriledgeApp : Application()